import 'package:flutter/material.dart';

class AddBuzzScreen extends StatelessWidget {
  const AddBuzzScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Add new buzz"),
    );
  }
}
